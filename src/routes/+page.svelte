<script lang="ts">
  // Phase 2 will replace this stub with the tagger UI.
</script>

<main>
  <h1>imxport tagger</h1>
  <p>The tagger UI is implemented in Phase 2.</p>
  <p>For now, run pipeline stages via the CLI:</p>
  <pre>pnpm imxport setup
pnpm imxport extract
pnpm imxport enrich</pre>
</main>

<style>
  main {
    font-family: system-ui, sans-serif;
    max-width: 40rem;
    margin: 4rem auto;
    padding: 0 1rem;
    line-height: 1.5;
  }
  pre {
    background: rgba(0, 0, 0, 0.06);
    padding: 0.75rem 1rem;
    border-radius: 6px;
  }
  @media (prefers-color-scheme: dark) {
    pre {
      background: rgba(255, 255, 255, 0.06);
    }
  }
</style>
